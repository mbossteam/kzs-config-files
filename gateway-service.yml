server:
  port: 8765

spring:
  application:
    name: gateway-service
  cloud:
    config:
      import-check:
        enabled: false
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

eureka:
  instance:
    instance-id: ${spring.application.name}:${server:port}
